using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TwistTileGrid : MonoBehaviour
{
    Grid twistGrid;
    // Start is called before the first frame update
    void Start()
    {
        twistGrid = GetComponent<Grid>();
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
